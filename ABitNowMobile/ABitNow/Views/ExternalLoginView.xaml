<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:behaviors="clr-namespace:ABitNow.Behaviors"
             x:Class="ABitNow.Views.ExternalLoginView"
             Title="{Binding Title}">
    <ContentPage.Content>
        <StackLayout>
            <WebView Source="{Binding SelectedExternalLoginProvider.Url}" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" 
                WidthRequest="1000" HeightRequest="1000">
                <WebView.Behaviors>
                    <behaviors:EventToCommandBehavior EventName="Navigated" Command="{Binding WebviewNavigatedCommand}" EventArgsConverter="{StaticResource LocalWebViewNavigatedConverter}">
                    </behaviors:EventToCommandBehavior>
                </WebView.Behaviors>
            </WebView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>